Proc For Checking if server is primary in an AG or will check primary if in a WFCI cluster or stand alone instance.
Proc for inserting the check primary step as step 1 to a list of jobs passed through.
Proc for removing the check primary step if deemed need it removed.  

These are fully documented in the proc and in a doc loaded here.
